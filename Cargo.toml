[package]
name = "codeseed"
version = "0.1.0"
edition = "2021"
description = "A CLI for managing agent skills inside a project."
license = "MIT"
repository = "https://github.com/seedware/codeseed"

[dependencies]
clap = { version = "4.5", features = ["derive"] }

